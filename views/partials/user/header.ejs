<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>SHADES - Premium Sunglasses</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script defer src="/script/script.js"></script>
    <link rel="icon" href="/favicon-16x16.png" type="image/png">
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"
      rel="stylesheet"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=Playfair+Display:wght@700&display=swap"
      rel="stylesheet"
    />
    <style>
      /* Apply Inter font globally, override Tailwind's font-sans */
      body {
        font-family: 'Inter', sans-serif;
      }

      /* Ensure Playfair Display for logo */
      .font-playfair {
        font-family: 'Playfair Display', serif;
      }

      /* Hide dropdowns by default */
      .dropdown-content {
        display: none;
      }

      /* Show dropdown when .show class is applied */
      .dropdown-content.show {
        display: block;
      }

      .profile-header1 {
        padding: 1rem;
        background-color: #ffffff;
        border-bottom: 1px solid #e5e7eb;
        display: flex;
        align-items: center;
        gap: 0.75rem;
        position: relative;
      }

      .profile-icon1 {
        width: 2.2rem;
        height: 2.2rem;
        background-color: #a1a1a1;
        border-radius: 9999px;
        display: flex;
        align-items: center;
        justify-content: center;
        position: relative;
        overflow: hidden;
      }

      .profile-image1 {
        width: 100%;
        height: 100%;
        object-fit: cover;
        border-radius: 9999px;
      }

      .username {
        color: #000000;
        font-weight: 700;
      }

      /* Mobile menu styles */
      #mobileMenu {
        transition: transform 0.3s ease-in-out;
        transform: translateX(100%);
      }

      #mobileMenu.show {
        transform: translateX(0);
      }
    </style>
  </head>

  <body>
    <!-- Header -->
    <header class="bg-white shadow-sm sticky top-0 z-50 md:pr-5 md:pl-5 xl:pl-10 xl:pr-10">
      <div class="containerr mx-auto px-4 py-4 flex justify-between items-center">
        <!-- Logo -->
        <div class="flex items-center">
          <a href="/" class="text-2xl font-bold tracking-wider text-gray-800 font-playfair"
            >SHADES</a
          >
        </div>

        <!-- Navigation -->
        <div class="hidden md:flex space-x-8">
          <a
            href="/"
            class="text-gray-600 hover:text-black font-medium font-inter transition duration-200 ease-in-out"
            >HOME</a
          >
          <a
            href="/shop"
            class="text-gray-600 hover:text-black font-medium font-inter transition duration-200 ease-in-out"
            >SHOP</a
          >
        </div>

        <!-- Action Icons -->
        <div class="flex items-center space-x-6">
          <!-- Search -->
          <div class="relative hidden md:block">
  <button
    onclick="goToShopSearch()"
    class="text-gray-600 hover:text-black transition duration-200 ease-in-out p-1"
  >
    <svg
      xmlns="http://www.w3.org/2000/svg"
      class="h-5 w-5"
      fill="none"
      viewBox="0 0 24 24"
      stroke="currentColor"
    >
      <path
        stroke-linecap="round"
        stroke-linejoin="round"
        stroke-width="2"
        d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"
      />
    </svg>
  </button>
</div>
          <!-- Wishlist Icon (add this right before or after your shopping cart) -->
          <a href="/wishlist" class="relative hidden md:block">
            <button
              class="text-gray-600 hover:text-black transition duration-200 ease-in-out p-1 relative"
            >
              <i class="fas fa-heart h-5 w-5"></i>

              <span
                id="wishlist-badge"
                class="absolute -top-1 -right-1 bg-red-600 text-white text-xs h-4 w-4 rounded-full hidden flex items-center justify-center"
              >
              </span>
            </button>
          </a>

          <!-- Shopping Cart -->
          <a href="/cart" class="relative inline-block hidden md:block">
            <button class="text-gray-600 hover:text-black transition duration-200 ease-in-out p-1">
              <i class="fas fa-shopping-cart h-5 w-5"></i>

              <!-- Dynamic badge -->
              <span
                id="cart-count-badge"
                class="absolute -top-1 -right-1 bg-red-600 text-white text-xs h-4 w-4 rounded-full hidden flex items-center justify-center"
              >
              </span>
            </button>
          </a>

          <!-- User Account Dropdown (Click Version) -->
          <div class="relative dropdown hidden md:block">
            <button
              class="text-gray-600 hover:text-black transition duration-500 ease-in-out p-1"
              onclick="toggleDropdown('userDropdown')"
            >
              <svg
                xmlns="http://www.w3.org/2000/svg"
                class="h-5 w-5"
                fill="none"
                viewBox="0 0 24 24"
                stroke="currentColor"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"
                />
              </svg>
            </button>
            <!-- User Dropdown -->
            <div
              id="userDropdown"
              class="dropdown-content absolute right-0 mt-2 w-48 bg-white rounded-lg shadow-lg py-2 transition-all duration-400 ease-in-out z-50"
            >
              <% if (!isLoggedIn) { %>
              <a
                href="/login"
                class="flex items-center gap-2 px-4 py-2 text-gray-600 hover:bg-black hover:text-white font-inter transition duration-400 ease-in-out"
              >
                <i class="fas fa-sign-in-alt w-4"></i> Log In
              </a>
              <a
                href="/signup"
                class="flex items-center gap-2 px-4 py-2 text-gray-600 hover:bg-black hover:text-white font-inter transition duration-400 ease-in-out"
              >
                <i class="fas fa-user-plus w-4"></i> Sign Up
              </a>

              <% } else { %>
              <span class="block text-gray-600 font-inter font-medium border-b border-gray-200">
                <!-- <i class="fas fa-user-circle mr-2 text-gray-500"></i><%= username %> -->
                <div class="profile-header1">
                  <div class="profile-icon1">
                    <% if (user.image) { %>
                    <img src="<%= user.image %>" alt="Profile Image" class="profile-image1" />
                    <% } else { %>
                    <i class="fas fa-user" style="color: #ffffff; font-size: 1.125rem"></i>
                    <% } %>
                  </div>
                  <div>
                    <div class="text-gray-700">Hello,</div>
                    <div class="username"><%= user.name %></div>
                  </div>
                </div>
              </span>

              <a
                href="/profile"
                class="flex items-center gap-2 px-4 py-2 text-gray-600 hover:bg-black hover:text-white font-inter transition duration-400 ease-in-out"
              >
                <i class="fas fa-user"></i> Profile
              </a>
              <a
                href="/address"
                class="flex items-center gap-2 px-4 py-2 text-gray-600 hover:bg-black hover:text-white font-inter transition duration-400 ease-in-out"
              >
                <i class="fas fa-map-marker-alt"></i> Address
              </a>
              <a
                href="/wishlist"
                class="flex items-center gap-2 px-4 py-2 text-gray-600 hover:bg-black hover:text-white font-inter transition duration-400 ease-in-out"
              >
                <i class="fas fa-heart"></i> Wishlist
              </a>
              <a
                href="/orders"
                class="flex items-center gap-2 px-4 py-2 text-gray-600 hover:bg-black hover:text-white font-inter transition duration-400 ease-in-out"
              >
                <i class="fas fa-box"></i> My Orders
              </a>
              <a
                href="/logout"
                class="flex items-center gap-2 px-4 py-2 text-gray-600 hover:bg-black hover:text-white font-inter transition duration-400 ease-in-out"
              >
                <i class="fas fa-sign-out-alt"></i> Logout
              </a>
              <% } %>
            </div>
          </div>

          <!-- Mobile Menu Button -->
          <button
            class="md:hidden text-gray-600 hover:text-black transition duration-200 ease-in-out p-1"
            onclick="toggleMobileMenu()"
          >
            <svg
              xmlns="http://www.w3.org/2000/svg"
              class="h-6 w-6"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M4 6h16M4 12h16M4 18h16"
              />
            </svg>
          </button>
        </div>
      </div>
    </header>

    <!-- Mobile Menu Overlay -->
    <div
      id="mobileMenuOverlay"
      class="fixed inset-0 bg-black bg-opacity-50 z-40 hidden md:hidden"
      onclick="closeMobileMenu()"
    ></div>

    <!-- Mobile Menu -->
    <div id="mobileMenu" class="fixed top-0 right-0 h-full w-80 bg-white shadow-lg z-50 md:hidden">
      <div class="flex justify-between items-center p-4 border-b">
        <h2 class="text-xl font-bold font-playfair">Menu</h2>
        <button onclick="closeMobileMenu()" class="text-gray-600 hover:text-black">
          <svg
            xmlns="http://www.w3.org/2000/svg"
            class="h-6 w-6"
            fill="none"
            viewBox="0 0 24 24"
            stroke="currentColor"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M6 18L18 6M6 6l12 12"
            />
          </svg>
        </button>
      </div>

      <nav class="p-4">
        <a
          href="/"
          class="block py-3 px-2 text-gray-600 hover:text-black font-medium border-b border-gray-100"
          >HOME</a
        >
        <a
          href="/shop"
          class="block py-3 px-2 text-gray-600 hover:text-black font-medium border-b border-gray-100"
          >SHOP</a
        >

        <!-- Account section -->
        <div class="mt-4">
          <% if (!isLoggedIn) { %>
          <a
            href="/login"
            class="block py-3 px-2 text-gray-600 hover:text-black font-medium border-b border-gray-100"
          >
            <i class="fas fa-sign-in-alt mr-2"></i> Log In
          </a>
          <a
            href="/signup"
            class="block py-3 px-2 text-gray-600 hover:text-black font-medium border-b border-gray-100"
          >
            <i class="fas fa-user-plus mr-2"></i> Sign Up
          </a>
          <% } else { %>
          <div class="py-3 px-2 border-b border-gray-100">
            <div class="flex items-center gap-3">
              <div class="profile-icon1" style="width: 1.5rem; height: 1.5rem">
                <% if (user.image) { %>
                <img src="<%= user.image %>" alt="Profile Image" class="profile-image1" />
                <% } else { %>
                <i class="fas fa-user" style="color: #ffffff; font-size: 0.75rem"></i>
                <% } %>
              </div>

              <div>
                <div class="text-sm text-gray-600">Hello,</div>
                <div class="font-bold text-black"><%= user.name %></div>
              </div>
            </div>
          </div>
          <a
            href="/profile"
            class="block py-3 px-2 text-gray-600 hover:text-black font-medium border-b border-gray-100"
          >
            <i class="fas fa-user mr-2"></i> Profile
          </a>
          <a
            href="/address"
            class="block py-3 px-2 text-gray-600 hover:text-black font-medium border-b border-gray-100"
          >
            <i class="fas fa-map-marker-alt mr-2"></i> Address
          </a>
          <!-- Wishlist Icon (add this right before or after your shopping cart) -->
          <a
            href="/wishlist"
            class="block py-3 px-1 text-gray-600 hover:text-black font-medium border-b border-gray-100"
          >
            <button
              class="text-gray-600 hover:text-black transition duration-200 ease-in-out p-1 relative"
            >
              <i class="fas fa-heart h-5 w-5"></i> Wishlist

              <span
                id="wishlist-badge"
                class="absolute -top-1 -right-1 bg-red-600 text-white text-xs h-4 w-4 rounded-full hidden flex items-center justify-center"
              >
              </span>
            </button>
          </a>

          <!-- Shopping Cart -->
          <a
            href="/cart"
            class="block py-3 px-1 text-gray-600 hover:text-black font-medium border-b border-gray-100"
          >
            <button class="text-gray-600 hover:text-black transition duration-200 ease-in-out p-1">
              <i class="fas fa-shopping-cart h-5 w-5"></i>Cart

              <!-- Dynamic badge -->
              <span
                id="cart-count-badge"
                class="absolute -top-1 -right-1 bg-red-600 text-white text-xs h-4 w-4 rounded-full hidden flex items-center justify-center"
              >
              </span>
            </button>
          </a>
          <a
            href="/orders"
            class="block py-3 px-2 text-gray-600 hover:text-black font-medium border-b border-gray-100"
          >
            <i class="fas fa-box mr-2"></i> My Orders
          </a>
          <a href="/logout" class="block py-3 px-2 text-gray-600 hover:text-black font-medium">
            <i class="fas fa-sign-out-alt mr-2"></i> Logout
          </a>
          <% } %>
        </div>
      </nav>
    </div>
  </body>
</html>
