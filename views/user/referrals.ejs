<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Refer Friends - SHADES</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script defer src="/script/script.js"></script>
    <link rel="icon" href="favicon/favicon.ico" type="ico">
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"
      rel="stylesheet"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=Playfair+Display:wght@700&display=swap"
      rel="stylesheet"
    />
    <style>
      :root {
        --primary-color: #191a1d;
      }

      body {
        font-family: "Inter", sans-serif;
        background-color: #f5f5f5;
      }

      .page-body {
        background-color: #ffffff;
      }

      .container {
        max-width: 95%;
        margin-left: auto;
        margin-right: auto;
        padding: 0.5rem;
        display: flex;
        flex-direction: column;
        gap: 1rem;
      }

      .sidebar {
        width: 100%;
        background-color: #ffffff;
        border-radius: 0.5rem;
        box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1),
          0 2px 4px -1px rgba(0, 0, 0, 0.06);
        height: fit-content;
        display: none;
      }

      .sidebar.active {
        display: block;
      }

      .sidebar-toggle {
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 0.5rem;
        background-color: var(--primary-color);
        color: white;
        border-radius: 0.25rem;
        cursor: pointer;
        font-size: 0.9rem;
        touch-action: manipulation;
      }

      .profile-header {
        padding: 1rem;
        border-bottom: 1px solid #e5e7eb;
        display: flex;
        align-items: center;
        gap: 0.5rem;
      }

      .profile-icon {
        width: 2.5rem;
        height: 2.5rem;
        background-color: #a1a1a1;
        border-radius: 9999px;
        display: flex;
        align-items: center;
        justify-content: center;
      }

      .profile-image {
        width: 100%;
        height: 100%;
        object-fit: cover;
        border-radius: 9999px;
      }

      .profile-icon i {
        color: #ffffff;
        font-size: 1rem;
      }

      .greeting {
        color: #6b7280;
        font-size: 0.8rem;
      }

      .username {
        color: #1f2937;
        font-weight: 600;
        font-size: 0.9rem;
      }

      .menu-link {
        display: flex;
        align-items: center;
        padding: 0.8rem 1rem;
        color: #000000;
        font-size: 0.8rem;
        touch-action: manipulation;
      }

      .menu-link:hover {
        background-color: #f9fafb;
      }

      .menu-link.active {
        background-color: #eff6ff;
        color: var(--primary-color);
      }

      .menu-icon {
        width: 1rem;
        margin-right: 0.5rem;
      }

      .logout-link {
        display: flex;
        align-items: center;
        padding: 0.8rem 1rem;
        color: #dc2626;
        font-size: 0.8rem;
        touch-action: manipulation;
      }

      .logout-link:hover {
        background-color: #fef2f2;
      }

      .main-content {
        flex: 1;
        background-color: #ffffff;
        border-radius: 0.5rem;
        box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1),
          0 2px 4px -1px rgba(0, 0, 0, 0.06);
        padding: 1rem;
      }

      .section-title {
        font-size: 1rem;
        font-weight: 600;
        color: #1f2937;
        margin-bottom: 1rem;
      }

      .referral-hero {
        color: #000000;
        padding: 0.75rem;
        border-radius: 0.75rem;
        margin-bottom: 0.5rem;
        text-align: center;
      }

      .referral-hero i {
        font-size: 1.5rem;
        margin-bottom: 0.5rem;
      }

      .referral-hero h1 {
        font-size: 1.25rem;
        font-weight: 700;
        margin-bottom: 0.25rem;
      }

      .referral-hero p {
        font-size: 0.9rem;
        opacity: 0.9;
      }

      .referral-code-card {
        background: linear-gradient(135deg, #1a1a1a 0%, #333333 100%);
        color: white;
        padding: 1rem;
        border-radius: 0.5rem;
        margin-bottom: 1rem;
      }

      .referral-code-row {
        display: flex;
        justify-content: space-between;
        align-items: center;
        flex-wrap: wrap;
        gap: 0.75rem;
        margin-bottom: 1rem;
      }

      .referral-code {
        font-size: 1.25rem;
        font-weight: 700;
        letter-spacing: 0.05em;
        background: rgba(255, 255, 255, 0.1);
        padding: 0.5rem 0.75rem;
        border-radius: 0.25rem;
        border: 2px dashed rgba(255, 255, 255, 0.3);
        display: inline-block;
        margin: 0.5rem 0;
      }

      .stats-item {
        flex: 1;
        text-align: center;
        min-width: 120px;
      }

      .stats-item h3 {
        font-size: 0.9rem;
        margin-bottom: 0.25rem;
      }

      .stats-item p {
        font-size: 1.25rem;
        font-weight: 700;
      }

      .btn {
        padding: 0.6rem 1.2rem;
        border: none;
        border-radius: 6px;
        cursor: pointer;
        font-size: 0.8rem;
        font-weight: 500;
        transition: all 0.2s ease;
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        touch-action: manipulation;
      }

      .btn-primary {
        background-color: #2563eb;
        color: white;
      }

      .btn-primary:hover {
        background-color: #1d4ed8;
        transform: translateY(-1px);
      }

      .btn-secondary {
        background-color: #ffffff;
        color: #1f2937;
        border: 1px solid #d1d5db;
      }

      .btn-secondary:hover {
        background-color: #f3f4f6;
      }

      .stats-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
        gap: 0.75rem;
        margin-bottom: 1.5rem;
      }

      .stats-card {
        background: linear-gradient(135deg, #f0f9ff 0%, #e0f2fe 100%);
        padding: 1rem;
        border-radius: 0.5rem;
        border: 1px solid #bae6fd;
        text-align: center;
      }

      .stats-number {
        font-size: 1.5rem;
        font-weight: 700;
        color: #0369a1;
        margin-bottom: 0.25rem;
      }

      .stats-label {
        color: #64748b;
        font-size: 0.8rem;
        font-weight: 500;
      }

      .referral-item {
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 0.75rem;
        border: 1px solid #e5e7eb;
        border-radius: 0.5rem;
        margin-bottom: 0.5rem;
        transition: all 0.2s ease;
        flex-wrap: wrap;
        gap: 0.5rem;
      }

      .referral-details {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        flex: 1;
      }

      .referral-avatar {
        width: 2rem;
        height: 2rem;
        border-radius: 9999px;
        background: #6b7280;
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        font-weight: 600;
        font-size: 0.9rem;
      }

      .badge {
        padding: 0.2rem 0.6rem;
        border-radius: 9999px;
        font-size: 0.7rem;
        font-weight: 500;
      }

      .badge-success {
        background-color: #22c55e;
        color: white;
      }

      .badge-pending {
        background-color: #f59e0b;
        color: white;
      }

      .how-it-works {
        background-color: #f8fafc;
        padding: 1rem;
        border-radius: 0.5rem;
        margin: 1.5rem 0;
      }

      .how-it-works h4 {
        font-weight: 600;
        margin-bottom: 0.5rem;
        color: #1f2937;
        font-size: 0.9rem;
      }

      .how-it-works p {
        font-size: 0.8rem;
        color: #374151;
      }

      .step {
        display: flex;
        align-items: flex-start;
        gap: 0.75rem;
        margin-bottom: 1rem;
      }

      .step-number {
        width: 1.5rem;
        height: 1.5rem;
        background: linear-gradient(135deg, #2563eb, #3b82f6);
        color: white;
        border-radius: 9999px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-weight: 600;
        font-size: 0.8rem;
        flex-shrink: 0;
      }

      .share-buttons {
        display: flex;
        gap: 0.5rem;
        flex-wrap: wrap;
        margin-top: 0.75rem;
      }

      .share-btn {
        padding: 0.4rem 0.8rem;
        border-radius: 0.25rem;
        font-size: 0.8rem;
        font-weight: 500;
        cursor: pointer;
        transition: all 0.2s ease;
        display: inline-flex;
        align-items: center;
        gap: 0.25rem;
        touch-action: manipulation;
      }

      .share-whatsapp {
        background-color: #25d366;
        color: white;
      }

      .share-whatsapp:hover {
        background-color: #128c7e;
      }

      .share-telegram {
        background-color: #0088cc;
        color: white;
      }

      .share-telegram:hover {
        background-color: #006ba6;
      }

      .share-copy {
        background-color: #374151;
        color: white;
      }

      .share-copy:hover {
        background-color: #1f2937;
      }

      .empty-state {
        text-align: center;
        padding: 2rem 0.5rem;
        color: #64748b;
      }

      .empty-state i {
        font-size: 3rem;
        margin-bottom: 0.75rem;
        color: #cbd5e1;
      }

      .empty-state p {
        font-size: 0.9rem;
      }

      @media (min-width: 768px) {
        .container {
          flex-direction: row;
          padding: 1rem;
          gap: 1rem;
        }

        .sidebar {
          width: 20rem;
          display: block;
        }

        .sidebar-toggle {
          display: none;
        }

        .main-content {
          padding: 1.5rem;
          padding-top: 0;
        }

        .section-title {
          font-size: 1.125rem;
          margin-bottom: 1.25rem;
        }

        .referral-hero {
          padding: 1rem;
          border-radius: 1rem;
        }

        .referral-hero i {
          font-size: 2rem;
          margin-bottom: 1rem;
        }

        .referral-hero h1 {
          font-size: 2rem;
          margin-bottom: 0.5rem;
        }

        .referral-hero p {
          font-size: 1rem;
        }

        .referral-code-card {
          padding: 1.5rem;
          border-radius: 0.75rem;
          margin-bottom: 1.5rem;
        }

        .referral-code-row {
          gap: 1rem;
          margin-bottom: 1.5rem;
        }

        .referral-code {
          font-size: 1.5rem;
          padding: 0.75rem 1rem;
          border-radius: 0.5rem;
          margin: 1rem 0;
          letter-spacing: 0.1em;
        }

        .stats-item {
          min-width: 150px;
        }

        .stats-item h3 {
          font-size: 1rem;
        }

        .stats-item p {
          font-size: 1.75rem;
        }

        .btn {
          padding: 0.75rem 1.5rem;
          font-size: 0.875rem;
        }

        .stats-grid {
          gap: 1rem;
          margin-bottom: 2rem;
          grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        }

        .stats-card {
          padding: 1.5rem;
          border-radius: 0.75rem;
        }

        .stats-number {
          font-size: 2rem;
          margin-bottom: 0.5rem;
        }

        .stats-label {
          font-size: 0.875rem;
        }

        .referral-item {
          padding: 1rem;
          margin-bottom: 0.75rem;
          flex-wrap: nowrap;
        }

        .referral-details {
          gap: 0.75rem;
        }

        .referral-avatar {
          width: 2.5rem;
          height: 2.5rem;
          font-size: 1rem;
        }

        .badge {
          padding: 0.25rem 0.75rem;
          font-size: 0.75rem;
        }

        .how-it-works {
          padding: 2rem;
          margin: 2rem 0;
        }

        .how-it-works h4 {
          font-size: 1rem;
          margin-bottom: 0.75rem;
        }

        .how-it-works p {
          font-size: 0.875rem;
        }

        .step {
          gap: 1rem;
          margin-bottom: 1.5rem;
        }

        .step-number {
          width: 2rem;
          height: 2rem;
          font-size: 1rem;
        }

        .share-buttons {
          gap: 0.75rem;
          margin-top: 1rem;
        }

        .share-btn {
          padding: 0.5rem 1rem;
          font-size: 0.875rem;
          gap: 0.5rem;
        }

        .empty-state {
          padding: 3rem 1rem;
        }

        .empty-state i {
          font-size: 4rem;
          margin-bottom: 1rem;
        }

        .empty-state p {
          font-size: 1rem;
        }

        .profile-header {
          padding: 1.25rem;
          gap: 0.75rem;
        }

        .profile-icon {
          width: 3rem;
          height: 3rem;
        }

        .profile-icon i {
          font-size: 1.125rem;
        }

        .greeting {
          font-size: 0.875rem;
        }

        .username {
          font-size: 1rem;
        }

        .menu-link,
        .logout-link {
          padding: 1rem 1.25rem;
          font-size: 0.875rem;
        }

        .menu-icon {
          width: 1.25rem;
          margin-right: 0.75rem;
        }
      }

      @media (max-width: 479px) {
        .section-title {
          font-size: 0.9rem;
        }

        .referral-hero {
          padding: 0.5rem;
          border-radius: 0.5rem;
        }

        .referral-hero i {
          font-size: 1.25rem;
          margin-bottom: 0.25rem;
        }

        .referral-hero h1 {
          font-size: 1rem;
          margin-bottom: 0.25rem;
        }

        .referral-hero p {
          font-size: 0.8rem;
        }

        .referral-code-card {
          padding: 0.75rem;
          border-radius: 0.5rem;
          margin-bottom: 0.75rem;
        }

        .referral-code-row {
          gap: 0.5rem;
          margin-bottom: 0.75rem;
        }

        .referral-code {
          font-size: 1rem;
          padding: 0.4rem 0.6rem;
          border-radius: 0.25rem;
          margin: 0.5rem 0;
          letter-spacing: 0.05em;
        }

        .stats-item {
          min-width: 100px;
        }

        .stats-item h3 {
          font-size: 0.8rem;
        }

        .stats-item p {
          font-size: 1rem;
        }

        .btn {
          padding: 0.5rem 1rem;
          font-size: 0.75rem;
        }

        .stats-grid {
          gap: 0.5rem;
          margin-bottom: 1rem;
          grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
        }

        .stats-card {
          padding: 0.75rem;
          border-radius: 0.5rem;
        }

        .stats-number {
          font-size: 1.25rem;
          margin-bottom: 0.25rem;
        }

        .stats-label {
          font-size: 0.75rem;
        }

        .referral-item {
          padding: 0.5rem;
          margin-bottom: 0.5rem;
        }

        .referral-details {
          gap: 0.4rem;
        }

        .referral-avatar {
          width: 1.75rem;
          height: 1.75rem;
          font-size: 0.8rem;
        }

        .badge {
          padding: 0.15rem 0.5rem;
          font-size: 0.65rem;
        }

        .how-it-works {
          padding: 0.75rem;
          margin: 1rem 0;
        }

        .how-it-works h4 {
          font-size: 0.8rem;
          margin-bottom: 0.5rem;
        }

        .how-it-works p {
          font-size: 0.75rem;
        }

        .step {
          gap: 0.5rem;
          margin-bottom: 0.75rem;
        }

        .step-number {
          width: 1.25rem;
          height: 1.25rem;
          font-size: 0.7rem;
        }

        .share-buttons {
          gap: 0.4rem;
          margin-top: 0.5rem;
        }

        .share-btn {
          padding: 0.3rem 0.6rem;
          font-size: 0.75rem;
          gap: 0.25rem;
        }

        .empty-state {
          padding: 1.5rem 0.5rem;
        }

        .empty-state i {
          font-size: 2.5rem;
          margin-bottom: 0.5rem;
        }

        .empty-state p {
          font-size: 0.8rem;
        }

        .profile-header {
          padding: 0.75rem;
        }

        .profile-icon {
          width: 2rem;
          height: 2rem;
        }

        .profile-icon i {
          font-size: 0.9rem;
        }

        .greeting,
        .username {
          font-size: 0.75rem;
        }

        .menu-link,
        .logout-link {
          padding: 0.6rem 0.8rem;
          font-size: 0.75rem;
        }

        .menu-icon {
          width: 0.9rem;
          margin-right: 0.4rem;
        }
      }
    </style>
  </head>
  <body class="page-body">
    <%- include("../../views/partials/user/header") %>

    <div class="container">
      <button class="sidebar-toggle md:hidden" onclick="toggleSidebar()">
        <i class="fas fa-bars mr-2"></i> Menu
      </button>
      <div class="sidebar" id="sidebar">
        <div class="profile-header">
          <div class="profile-icon">
            <% if (user.image) { %>
              <img src="<%= user.image %>" alt="Profile Image" class="profile-image"
                   onerror="this.src='https://via.placeholder.com/80?text=Profile'" />
            <% } else { %>
              <i class="fas fa-user"></i>
            <% } %>
          </div>
          <div>
            <div class="greeting">Hello,</div>
            <div class="username"><%= user.name %></div>
          </div>
        </div>
        <nav class="menu">
          <ul class="menu-list">
            <li class="menu-item">
              <a href="/profile" class="menu-link">
                <i class="fas fa-user menu-icon"></i> Profile Information
              </a>
            </li>
            <li class="menu-item">
              <a href="/orders" class="menu-link">
                <i class="fas fa-box menu-icon"></i> My Orders
              </a>
            </li>
            <li class="menu-item">
              <a href="/address" class="menu-link">
                <i class="fas fa-home menu-icon"></i> Manage Addresses
              </a>
            </li>
            <li class="menu-item">
              <a href="/wallet" class="menu-link">
                <i class="fas fa-wallet menu-icon"></i> My Wallet
              </a>
            </li>
            <li class="menu-item">
              <a href="/wishlist" class="menu-link">
                <i class="fas fa-heart menu-icon"></i> My Wishlist
              </a>
            </li>
            <li class="menu-item">
              <a href="/referrals" class="menu-link active">
                <i class="fas fa-users menu-icon"></i> Refer Friends
              </a>
            </li>
            <li class="menu-item">
              <a href="/changepassword" class="menu-link">
                <i class="fas fa-lock menu-icon"></i> Change Password
              </a>
            </li>
            <li class="menu-item">
              <a href="/logout" class="logout-link">
                <i class="fas fa-sign-out-alt menu-icon"></i> Log Out
              </a>
            </li>
          </ul>
        </nav>
      </div>

      <div class="main-content">
        <div class="referral-hero">
          <i class="fas fa-gift"></i>
          <h1>Refer Friends & Earn Rewards</h1>
          <p>Invite your friends and both of you get Rewards!</p>
        </div>

        <div class="referral-code-card">
          <div class="referral-code-row">
            <div class="stats-item">
              <h3><i class="fas fa-code mr-2"></i>Your Referral Code</h3>
              <div id="referralCode" class="referral-code"><%= referralCode %></div>
            </div>
            <div class="stats-item">
              <h3>Total Referrals</h3>
              <p id="totalReferrals"><%= totalReferrals %></p>
            </div>
            <div class="stats-item">
              <h3>Total Earned</h3>
              <p id="totalEarned">₹<%= totalEarned %></p>
            </div>
          </div>
          <div class="share-buttons">
            <button class="share-btn share-copy" onclick="copyReferralCode()">
              <i class="fas fa-copy"></i> Copy Code
            </button>
            <button class="share-btn share-whatsapp" onclick="shareOnWhatsApp()">
              <i class="fab fa-whatsapp"></i> WhatsApp
            </button>
            <button class="share-btn share-telegram" onclick="shareOnTelegram()">
              <i class="fab fa-telegram"></i> Telegram
            </button>
          </div>
          <p>Share this code with friends or use the share buttons above</p>
        </div>

        <div class="how-it-works">
          <h4><i class="fas fa-lightbulb mr-2 text-yellow-500"></i>How It Works</h4>
          <p>Share your referral code with friends. When they sign up using your code, both you and your friend will receive rewards! You will get ₹220.00 and your friend will get ₹100.00.</p>
        </div>

        <div>
          <h3 class="section-title">
            <i class="fas fa-users mr-2 text-blue-600"></i>Your Referrals
          </h3>
          <% if (referrals.length === 0) { %>
            <div class="empty-state">
              <i class="fas fa-user-friends"></i>
              <p>No referrals yet. Share your code and earn rewards!</p>
            </div>
          <% } else { %>
            <% referrals.forEach(r => { %>
              <div class="referral-item">
                <div class="referral-details">
                  <div class="profile-icon">
                    <% if (r.image) { %>
                      <img src="/Uploads/userProfileimages/<%= r.image %>" alt="Profile Image" class="profile-image"
                           onerror="this.src='https://via.placeholder.com/80?text=Profile'" />
                    <% } else { %>
                      <i class="fas fa-user"></i>
                    <% } %>
                  </div>
                  <div>
                    <p class="font-semibold"><%= r.name %></p>
                    <p class="text-gray-600">Joined on <%= new Date(r.createdAt).toDateString() %></p>
                  </div>
                </div>
                <div class="text-right">
                  <span class="badge badge-success">₹100 Earned</span>
                  <p class="text-gray-600 text-sm mt-1 pr-4">Completed</p>
                </div>
              </div>
            <% }) %>
          <% } %>
        </div>
      </div>
    </div>

    <div id="toastContainer"></div>

    <%- include("../../views/partials/user/footer") %>

    <script>
      function toggleSidebar() {
        const sidebar = document.getElementById("sidebar");
        sidebar.classList.toggle("active");
      }

      function copyReferralCode() {
        const referralCode = document.getElementById('referralCode').textContent;
        navigator.clipboard.writeText(referralCode).then(() => {
          showToast('Referral code copied to clipboard!', 'success');
        }).catch(() => {
          showToast('Failed to copy code', 'error');
        });
      }

      function shareOnWhatsApp() {
        const referralCode = document.getElementById('referralCode').textContent;
        const message = `Hey! I'm using SHADES for amazing eyewear. Use my referral code "${referralCode}" when you sign up and get ₹100! Check it out: https://shades.com/signup`;
        const whatsappUrl = `https://wa.me/?text=${encodeURIComponent(message)}`;
        window.open(whatsappUrl, '_blank');
      }

      function shareOnTelegram() {
        const referralCode = document.getElementById('referralCode').textContent;
        const message = `Hey! I'm using SHADES for amazing eyewear. Use my referral code "${referralCode}" when you sign up and get ₹100! Check it out: https://shades.com/signup`;
        const telegramUrl = `https://t.me/share/url?url=https://shades.com/signup&text=${encodeURIComponent(message)}`;
        window.open(telegramUrl, '_blank');
      }

      document.addEventListener('DOMContentLoaded', function() {
        console.log('Referral page loaded');
      });
    </script>
  </body>
</html>